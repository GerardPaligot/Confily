source "https://rubygems.org"

gem "fastlane"
# Pulled in transitively by fastlane's Google Play client (representable),
# but no longer declared as a runtime dependency there, so it must be listed
# explicitly or `require "multi_json"` fails under Bundler on Ruby 3.3+.
gem "multi_json"
