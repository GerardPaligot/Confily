CREATE TABLE Menu (
name TEXT NOT NULL PRIMARY KEY,
dish TEXT NOT NULL,
accompaniment TEXT NOT NULL,
dessert TEXT NOT NULL
);

insertMenu:
INSERT OR REPLACE INTO Menu(
name, dish, accompaniment, dessert
) VALUES (?, ?, ?, ?);

selectMenus:
SELECT * FROM Menu;
